spring:
  datasource:
    url: "jdbc:h2:mem:ludence;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE"
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop

storage:
  image-directory: ./build/test-images

auth:
  jwt:
    secret: test-jwt-secret
    expiration-seconds: 3600
