server:
  tomcat:
    threads:
      max: 200
      min-spare: 20
    accept-count: 20

spring:
  application:
    name: ludens
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/ludens}
    username: ${DB_USERNAME:ludens}
    password: ${DB_PASSWORD:ludens}
    hikari:
      maximum-pool-size: 20
      connection-timeout: 2000
  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:update}
    open-in-view: false
  servlet:
    multipart:
      max-file-size: 2MB

storage:
  image-directory: ${IMAGE_STORAGE_DIRECTORY:./data/images}

auth:
  jwt:
    secret: ${JWT_SECRET:ludens-local-jwt-secret-change-me}
    expiration-seconds: ${JWT_EXPIRATION_SECONDS:86400}
